# Environment Variables Configuration

# ============================================
# CLOUD SERVICE (Railway/Heroku)
# ============================================

# Local service URL (set this to your store computer's public IP or use ngrok)
LOCAL_SERVICE_URL=http://localhost:8080

# API key for secure communication between cloud and local service
LOCAL_API_KEY=your-secret-api-key-change-this

# Database URL (automatically set by Railway/Heroku when you add PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database

# Port (automatically set by Railway/Heroku)
# PORT=5000


# ============================================
# LOCAL SERVICE (Store Computer)
# ============================================

# API key (must match the cloud service)
LOCAL_API_KEY=your-secret-api-key-change-this

# COM port for ESP32 device
COM_PORT=COM3

# Port for local service (optional, auto-detects if not set)
# PORT=8080
